# Release {{RELEASE_NAME}}

This release bundles Raspberry Pi images built by pi-gen with OpenCarDev customisations (stage60).

### SBOM

CycloneDX and SPDX SBOMs are attached for each image (generated by syft).
If available, a package inventory (sbom-packages.txt) generated during the build is also attached.

### Installation

Follow the installation guide: {{INSTALL_DOC_URL}}

### Changelog

The changelog for this image is included below if present in the repository.